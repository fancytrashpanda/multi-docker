sudo: required
services:
  - docker

before_install:
  - docker build -t kaitlinsickles/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true kaitlinsickles/react-test npm test

after_success:
  - docker build -t kaitlinsickles/multi-client ./client
  - docker build -t kaitlinsickles/multi-nginx ./nginx
  - docker build -t kaitlinsickles/multi-server ./server
  - docker build -t kaitlinsickles/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  
  # Take those images and push them to docker hub
  - docker push kaitlinsickles/multi-client
  - docker push kaitlinsickles/multi-nginx
  - docker push kaitlinsickles/multi-server
  - docker push zksickles/multi-worker